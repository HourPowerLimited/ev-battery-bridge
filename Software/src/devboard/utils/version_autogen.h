#pragma once

// Static version header — update on release or when GitHub Actions is wired up.
// To use tagged versions: restore tools/identify_build.py as a CI-only pre-script.

#define GIT_ANCESTOR_TAG "dev"
#define GIT_SHA "dev"
#define GIT_SHORT_SHA "dev"
#define GIT_BRANCH "devkit-canfd-interface"
